# 구현 및 운영 상태

2026-09-13 · 기존 Obsidian 일일 브리핑 프로젝트를 이어 개선했습니다.

## 현재 구성

- 기존 `옵시디언_iCloudSync` 프로젝트와 `tech-ai-briefing-08`의 오전 8시 일정을 유지합니다. 프로젝트 ID는 `c9d65b6f-2fcd-49ba-a120-3fce98a14cf3`입니다. 새 프로젝트 등록은 필요 없습니다.
- 실제 소스 저장소는 `/Users/shinjh/Projects/Personal/Apps/tech-knowledge-garden`, 현재 콘텐츠 원본은 그 안의 `vault/`입니다. 기존 프로젝트에 이 경로와 작업 절차를 연결했습니다.
- 웹 기본 메뉴는 뉴스·브리핑·연결 지도입니다. 홈, 기사와 개념 페이지에도 지도를 표시하고 노드에서 관련 뉴스를 읽을 수 있습니다. 원본 보관용 Editions, Archive, Trends, 운영 문서와 폴더 탐색기는 웹·검색에서 제외됩니다.
- 브리핑 113회, 사건별 뉴스 42개, 개념 23개를 제공합니다. 기존 원본 Editions와 Archive는 이번 수정에서 변경하지 않았습니다. 최초 이전 당시 165개 파일의 복구 사본도 유지합니다.
- 앞선 개념 재구축에서 원문으로 검토한 정의·키워드와 31개 관계의 근거를 보존했습니다. 새 연결에는 대상 ID와 확인한 연관성의 이유만 필요합니다. 방향·참조 유형·인용은 필수가 아닙니다. 과거 뉴스 이력은 당시 날짜의 기록으로 보존했습니다.
- Sigma.js 3의 WebGL, Graphology ForceAtlas2와 Louvain을 도입했습니다. 작은 점과 가는 무방향 선을 연결 관계에 따라 배치합니다. 실제 개념 23개·키워드 101개·뉴스 42개, 총 166개 노드와 323개 연결입니다. 안내·섹션명·출처 목록에서 우연히 일치한 키워드는 기사 연결에 포함하지 않습니다.
- Obsidian 지도는 확인된 개념 간 관계를 유지하고 웹 지도는 실제 키워드·뉴스로 확장합니다. 노드를 선택하면 최대 두 번의 연결로 닿는 뉴스를 보여 주고 간접 연결의 경유 개념·키워드를 표시합니다. 검색·연결 필터·이동·확대·드래그·맞춤·Worker 재배치와 키보드 목록을 지원합니다. WebGL이 없으면 검색·목록으로 기사를 읽을 수 있습니다.
- 별칭, 상대 경로, 한글 정규화, 문단과 블록 링크를 웹 주소로 변환합니다. 해석할 수 없거나 모호한 링크는 빌드에서 실패합니다.
- RSS 구독 화면에 복사 가능한 피드 주소를 제공하고 모든 페이지에 자동 발견 링크를 추가했습니다. XML의 한글은 숫자 문자 참조로 기록하여 브라우저의 잘못된 문자셋 추측에도 구독 데이터가 보존됩니다.

## 검증

- 전체 테스트 188개, TypeScript 검사, 스킬 형식 검사와 스킬 검증기 테스트 4개 통과.
- HTML 184개, 검색 문서 182개, 그래프 노드 166개·연결 323개, RSS 항목 40개 검사 통과. 내부 링크·문단 ID·자산·RSS 도착 주소·제외 경로와 기사 연결의 근거를 검사했습니다.
- 실제 WebGL 화면에서 점 선택 → 관련 뉴스 → 기사 열기 → 지도 복귀, 홈 검색 → 선택 상태를 유지한 전체 지도, 연결 필터·확대·배경 이동·노드 드래그·Worker 재배치·키보드 이동을 확인했습니다.
- 데스크톱과 390px 휴대폰 너비에서 확인했습니다. 가로 넘침이 없었고 수정 후 브라우저 콘솔 오류·경고는 없었습니다. 별도 Chromium에서 WebGL을 비활성화해 목록 대체 화면과 MCP 선택 → Agents API 기사 링크를 확인했습니다. 실제 휴대폰의 터치·GPU 성능 시험은 아닙니다.
- Node v26.4.0/macOS 배치 계산: 현재 166개 노드·323개 연결·600회는 58ms, 합성 1,000개 노드·2,200개 연결·600회는 1,262ms였습니다. 모든 좌표가 유한했습니다. 이는 배치 계산만의 측정이며 실제 브라우저 프레임 속도를 뜻하지 않습니다. `node scripts/benchmark-connections.mjs`로 재현합니다.
- 빌드·검사 기록은 `.local/evidence/connections-*`, 복구 사본은 `.local/knowledge-before-rebuild/`에 있습니다. 일회성 재구축 스크립트는 재실행을 차단합니다.

## 발행

발행 주소는 [기존 뉴스 사이트](https://skyan0213.github.io/tech-knowledge-garden/), [RSS 구독](https://skyan0213.github.io/tech-knowledge-garden/rss), [지식 지도](https://skyan0213.github.io/tech-knowledge-garden/knowledge-maps/ai-technology-knowledge-map)입니다. `main`의 변경을 [Publish Garden](https://github.com/SKYAN0213/tech-knowledge-garden/actions/workflows/publish.yaml)이 검사하고 배포합니다. 로컬 빌드와 실제 배포 결과를 구분하여 해당 커밋의 실행 결과와 공개 주소를 확인합니다.

이전 독자 화면 정리의 공개 검증: [배포 실행 34726929373](https://github.com/SKYAN0213/tech-knowledge-garden/actions/runs/34726929373)이 코드 커밋 `7136cb3`으로 성공했습니다. 당시 홈·RSS·최신 회차·개념·지도는 HTTP 200, 제외한 Archive·Editions 경로는 404를 반환했습니다. 이 기록은 새 WebGL 연결 지도의 배포 증거와 구분합니다. 상세 응답은 `.local/evidence/revision-live.json`에 있습니다.

WebGL 연결 지도 공개 검증: 코드 커밋 `06ab001`의 [배포 실행 34732605328](https://github.com/SKYAN0213/tech-knowledge-garden/actions/runs/34732605328)이 성공했습니다. 공개 홈의 지도, 실제 점 선택 → 에이전트 보안 관련 뉴스 10개 → Copilot 기사 열기 → 지도 복귀, Worker 재배치를 확인했습니다. 공개 그래프는 166개 노드·323개 연결, RSS는 한글 제목을 가진 40개 항목이며, 홈·지도·기사·최신 브리핑·Worker·RSS는 200, Archive·Editions는 404였습니다. 콘솔 오류·경고는 없었습니다. HTTP 응답 기록은 `.local/evidence/connections-live.json`입니다. 그래픽 미지원 목록 화면은 빈 캔버스 공간을 접어 검색과 기사가 바로 이어지도록 다듬었습니다.

## 검증 범위

기존 오전 8시 자동화가 작성한 9월 13일 원고를 보존했고, 다음 취재는 마지막 cutoff `2026-09-13T08:01:40+09:00` 이후부터 이어집니다. 새 운영 지시를 예약에 반영했지만 다음 예약 시간이 실행된 것은 아닙니다. 이번 개념 재검토가 모든 과거 뉴스를 재검증한 것은 아닙니다.

현재 대화 자체를 다른 저장 프로젝트에 재지정하는 앱 API는 없습니다. 예약 작업은 이미 사용자가 지정한 기존 프로젝트에 속하며, 별도 등록 대기 상태가 아닙니다. 이전 문서의 새 프로젝트 등록 필요 설명은 정정했습니다.

별도 AI API 비용이나 유료 자동화 서비스는 추가하지 않았습니다. GPT 조사는 기존 ChatGPT/Codex 구독 사용량을 이용하며 로컬 예약에는 Mac과 Codex 앱이 켜져 있어야 합니다. 공개 저장소의 웹 출력 제외 파일을 비공개 자료로 취급하지 않습니다.
